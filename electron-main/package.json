{
  "name": "plantuml-editor",
  "description": "",
  "main": "dist/main.js",
  "scripts": {
    "start": "npx electron .",
    "build": "npx tsc",
    "webpack": "cd ../electron-renderer && npm run webpack",
    "clean": "rm dist/*.js",
    "package": "npx electron-packager . 'PlantUML Editor' --platform=darwin --overwrite --arch=x64",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^13.13.4",
    "child_process": "^1.0.2",
    "electron": "^9.4.0",
    "google-protobuf": "^3.14.0",
    "grpc": "npm:@grpc/grpc-js@^1.0.3"
  },
  "devDependencies": {
    "electron-packager": "^15.2.0",
    "grpc-tools": "^1.8.1",
    "typescript": "^3.8.3"
  }
}
